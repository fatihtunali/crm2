# ============================================
# CENTRALIZED ENVIRONMENT CONFIGURATION
# ============================================
# All environment variables are validated at startup via src/lib/env.ts
# The application will fail fast with clear error messages if required variables are missing
# This ensures environment issues are caught immediately rather than at runtime

# Database Configuration
# Replace these with your actual database credentials
# REQUIRED: All database variables must be set
DATABASE_HOST=your_database_host
DATABASE_PORT=3306
DATABASE_USER=your_database_user
DATABASE_PASSWORD=your_secure_database_password
DATABASE_NAME=your_database_name

# JWT Secret for Authentication
# REQUIRED: Generate a strong secret with: openssl rand -base64 32
# SECURITY: This MUST be at least 32 characters long - the application will fail to start without it
JWT_SECRET=your_jwt_secret_at_least_32_characters_long_replace_this_with_generated_secret

# App Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# AI Configuration (Anthropic Claude)
# REQUIRED: Get your API key from: https://console.anthropic.com/
# The key should start with "sk-ant-"
ANTHROPIC_API_KEY=your_anthropic_api_key

# Optional: Redis for caching and rate limiting
# REDIS_URL=redis://localhost:6379

# Optional: Error monitoring (Sentry)
# SENTRY_DSN=your_sentry_dsn

# Optional: Email configuration for notifications
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_smtp_password
# SMTP_FROM=noreply@example.com

# Optional: Logging level
# LOG_LEVEL=info
